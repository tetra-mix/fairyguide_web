export type content = {
  title: string,
  summary: string,
  year: number,
  month: number,
  day: number
}
export type contentCard = {
  title: string,
  summary: string,
  year: number,
  month: number,
  day: number
  url: string
}